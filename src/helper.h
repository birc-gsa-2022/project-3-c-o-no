void printIntArray(int * a, int len) {
    for(int i=0; i<len; i++) {
        printf("%d", a[i]);
    }
    printf("\n");
}

void printString(char * a, int len) {
    for(int i=0; i<len; i++) {
        printf("%c", a[i]);
    }
    printf("\n");
}